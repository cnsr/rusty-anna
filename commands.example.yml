commands:
    example:
        description: "example command"
        regex: "^\.test$"
        replies:
            - "hello"
            - "hi"
            - "test successful"
    weather:
        description: "get weather by location"
        regex: "^\.w(eather)? (.+)$"
        execute: "weather"